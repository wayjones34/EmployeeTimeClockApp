<Window x:Class="EmployeeTimeClockApp.ResetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reset Password"
        Height="360" Width="460"
        WindowStartupLocation="CenterOwner"
        Background="#F3F4F6">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Text="New Password" Margin="0,0,0,4"/>
        <PasswordBox x:Name="NewPasswordBox"
                     Grid.Row="2"
                     Height="28"
                     PasswordChanged="PasswordBoxes_Changed"/>

        <TextBlock Grid.Row="3" Text="Confirm Password" Margin="0,10,0,4"/>
        <PasswordBox x:Name="ConfirmPasswordBox"
                     Grid.Row="4"
                     Height="28"
                     PasswordChanged="PasswordBoxes_Changed"/>

        <!-- Rules -->
        <Border Grid.Row="5"
                Margin="0,12,0,0"
                Padding="10"
                Background="White"
                CornerRadius="8"
                BorderBrush="#E5E7EB"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Password Rules"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>

                <TextBlock x:Name="RuleLength"  Text="• At least 8 characters"/>
                <TextBlock x:Name="RuleUpper"   Text="• At least 1 uppercase letter (A-Z)"/>
                <TextBlock x:Name="RuleLower"   Text="• At least 1 lowercase letter (a-z)"/>
                <TextBlock x:Name="RuleNumber"  Text="• At least 1 number (0-9)"/>
                <TextBlock x:Name="RuleSpecial" Text="• At least 1 special character (@#$% etc.)"/>
                <TextBlock x:Name="RuleMatch"   Text="• Passwords match" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button x:Name="ResetButton"
                    Content="Reset"
                    Width="55"
                    Click="Reset_Click"
                    IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>
