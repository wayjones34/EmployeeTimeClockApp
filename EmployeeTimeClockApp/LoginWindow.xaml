<Window x:Class="EmployeeTimeClockApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login - Employee Time Clock"
        Height="340"
        Width="420"
        MinHeight="320"
        MinWidth="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F3F4F6">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Please sign in"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,12"/>

        <!-- Card -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="8"
                Padding="16"
                BorderBrush="#E5E7EB"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Username -->
                <StackPanel Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock x:Name="LoggedInUserText"
           FontWeight="SemiBold"
           FontSize="14"
           Margin="0,0,0,8"
           Text="Logged in as:" />

                    <TextBox x:Name="UsernameTextBox"
                             Height="30"
                             Padding="4"
                             HorizontalAlignment="Stretch"/>
                </StackPanel>

                <!-- Password -->
                <StackPanel Grid.Row="1" Margin="0,4,0,8">
                    <TextBlock Text="Password"
                               FontWeight="SemiBold"
                               FontSize="13"
                               Margin="0,0,0,4"/>
                    <PasswordBox x:Name="PasswordBox"
                                 Height="30"
                                 Padding="4"
                                 HorizontalAlignment="Stretch"/>
                </StackPanel>

                <!-- Buttons (Submit + Cancel) -->
                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,16,0,0">
                    <Button Content="Cancel"
                            Width="90"
                            Height="30"
                            Margin="0,0,8,0"
                            Click="CancelButton_Click"
                            IsCancel="True"/>

                    <!-- ✅ This is your SUBMIT / LOGIN button -->
                    <Button Content="Login (Submit)"
                            Width="110"
                            Height="30"
                            Background="#2563EB"
                            Foreground="White"
                            FontWeight="SemiBold"
                            BorderThickness="0"
                            Click="LoginButton_Click"
                            IsDefault="True"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Error message -->
        <TextBlock x:Name="ErrorText"
                   Grid.Row="2"
                   Foreground="#DC2626"
                   Margin="0,8,0,0"
                   Text=""
                   HorizontalAlignment="Left"/>
    </Grid>
</Window>
