<Window x:Class="EmployeeTimeClockApp.CreateAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Account"
        Height="340" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="#F3F4F6">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Create Employee Account"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,12"/>

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="8"
                Padding="12"
                BorderBrush="#E5E7EB"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Employee" FontWeight="SemiBold" Margin="0,0,8,10"/>
                <ComboBox x:Name="EmployeeComboBox"
                          Grid.Row="0" Grid.Column="1"
                          Height="28"
                          Margin="0,0,0,10"
                          DisplayMemberPath="FullName"
                          SelectedValuePath="EmployeeId"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Username" FontWeight="SemiBold" Margin="0,0,8,10"/>
                <TextBox x:Name="UsernameTextBox"
                         Grid.Row="1" Grid.Column="1"
                         Height="28"
                         Margin="0,0,0,10"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Password" FontWeight="SemiBold" Margin="0,0,8,10"/>
                <PasswordBox x:Name="PasswordBox"
                             Grid.Row="2" Grid.Column="1"
                             Height="28"
                             Margin="0,0,0,10"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Role" FontWeight="SemiBold" Margin="0,0,8,0"/>
                <ComboBox x:Name="RoleComboBox"
                          Grid.Row="3" Grid.Column="1"
                          Height="28">
                    <ComboBoxItem Content="Employee" IsSelected="True"/>
                    <ComboBoxItem Content="Admin"/>
                </ComboBox>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel"
        Width="100"
        Margin="0,0,8,0"
        Click="Cancel_Click"/>
            <Button Content="Create Account"
        Width="140"
        Click="Create_Click"/>
        </StackPanel>
    </Grid>
</Window>
